#include <bits/stdc++.h>
using namespace std;

#define rep(i, n) for (int i = 0; i < (n); i++)
using ll = long long;

int main() {
    int n;
    cin >> n;

    double ans = (9.0 * n / 5.0) + 32.0;
    cout << ans << endl;

    return 0;
}
